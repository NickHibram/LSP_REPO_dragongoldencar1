AI Tools Used:
ChatGPT

Prompts Used (2–5 max):
1. I agree that nextId as the it is the int counter used to assign unique IDs and requests the Array that stores all requests areshared resouces I agree that if mutlipe people call addRequest it will result in and this race could result in nextId and request getting overloaded and I agreee that it is unsafe as they are not sycronized For example, two threads can both call getNextId() and read the same value of nextId before either increments it Additionally, ArrayList is not thread-safe, so concurrent add() calls could lead to corrupting the list Make sure that it is in the correct format of : Part 1: Shared Resource #1: Shared Resource #2: Concurrency Problem: Why addRequest() is unsafe:
2. Lets move on to part 2: You are a strict reviewer. Check the following work for accuracy. Think step by step This is my thoughts: I think the fix A is still not correct as it does not fix the problem with the requeests only the nextID similar mindset for fix C you're I don't believe you're actually fixing both probelms I think B is the correct answer as it includes both addrequest and the nextID THis is the code: import java.util.ArrayList; import java.util.List; public class RequestManager { private int nextId = 1; private List<String> requests = new ArrayList<>(); public int getNextId() { int id = nextId; nextId++; return id; } public void addRequest(String studentName) { int id = getNextId(); String request = "Request-" + id + " from " + studentName; requests.add(request); } public List<String> getRequests() { return requests; } } Part 2: Evaluate Fixes (10 points) For each fix, determine whether it correctly solves the concurrency problem. • If the fix is correct, explain why it works. • If the fix is not correct, explain what problem still exists. There are no partially correct answers for this section. Fix A: public synchronized int getNextId() { ... } Fix B: public synchronized void addRequest(String studentName) { ... } Fix C: public synchronized List<String> getRequests() { ... }
3. For part 3: Part 3: Object-Oriented Design (5 points) 1. Based on Arthur’s Riel’s heuristics, should getNextId() be public? 2. Explain. I think no it should not be public. Becuase Arthur’s Riel’s heuristics state that classes should be encapuslated or hide their interal workings and only expose what people trully need. getNextId is interal and vital to make the program work What are your thoughts
4. For part 4 In lecture, we discussed an alternative approach to managing concurrency that does not rely on the synchronized keyword. 1. Describe how this alternative approach could be used to make addRequest() thread-safe. 2. Provide a short code snippet (5–10 lines) demonstrating how this approach would be applied to make addRequest()thread-safe. format: Part 4: Description: <your answer> Code Snippet: <your answer> I forgot what it was called but I think it should be something that is able to have priority or hold the thread while it needs it so other things can not interfere

How AI Helped:
AI helped me check my reasoning step-by-step and confirm whether my answers about concurrency and synchronization were correct. It also helped me structure my responses clearly in the required format and recall concepts like locks that I partially remembered.

Reflection:
I learned that thread safety requires protecting the entire critical section, not just one part of it. I also better understand how synchronization and locks solve race conditions.